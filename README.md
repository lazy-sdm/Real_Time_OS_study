# Real_Time_OS_study
honey_orange

Based on rtems run by docker. (gr712rc-qual-only)

Clear the underbar (_) and run it with init.c.


Enter the following command for execution

$ make
$ cd ./b-gr712rc-qual-only/
$ sparc-rtems6-sis -leon3 -dumbio -r s app.exe
